#include <iostream>
#include <vector>

using namespace std;

// Primero vamos a entender 
const int FILAS = 20;
const int COLUMNAS = 20;

enum TERRENO {LIBRE = 0, EDIFICIO = 1, AGUA = 2, BLOQUEADO = 3 };
